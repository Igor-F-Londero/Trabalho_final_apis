name: trabalho-final

services:
  api1:
    build: .
    restart: on-failure
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/livros?schema=public
      RABBITMQ_URL: amqp://rabbitmq:5672
      PORT: 3000
    depends_on:
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  api2:
    build: .
    restart: on-failure
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/livros?schema=public
      RABBITMQ_URL: amqp://rabbitmq:5672
      PORT: 3000
    depends_on:
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  api3:
    build: .
    restart: on-failure
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/livros?schema=public
      RABBITMQ_URL: amqp://rabbitmq:5672
      PORT: 3000
    depends_on:
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  consumer-notifications:
    build: .
    restart: on-failure
    command: node src/messaging/consumers/notificationsConsumer.js
    environment:
      RABBITMQ_URL: amqp://rabbitmq:5672
      CONSUMER_DELAY_MS: ${CONSUMER_DELAY_MS:-4000}
    depends_on:
      rabbitmq:
        condition: service_healthy

  consumer-audit:
    build: .
    restart: on-failure
    command: node src/messaging/consumers/audit_log.js
    environment:
      RABBITMQ_URL: amqp://rabbitmq:5672
      CONSUMER_DELAY_MS: ${CONSUMER_DELAY_MS:-4000}
    depends_on:
      rabbitmq:
        condition: service_healthy

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    restart: on-failure
    ports:
      - "8080:80"
    depends_on:
      - api1
      - api2
      - api3

  db:
    image: postgres:16
    restart: on-failure
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: livros
    volumes:
      - livros_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3-management
    restart: on-failure
    ports:
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 5s
      timeout: 10s
      retries: 10
      start_period: 30s

volumes:
  livros_data:
